# AGENTS.md

本文件是 **AI 助手（opencode / 其他 AI 代理）** 在此项目工作时必须遵守的约定。
它由项目维护者制定，AI 每次会话开始时应重新读取本文件。

---

## 1. 工作总则

- **每次代码或文档改动后，必须自动 commit + push**。
- 改动分类：
  - **代码 / 配置 / 资源** 改动 → 触发 commit。
  - **玩法 / 策略 / 数值 / 系统设计** 改动 → 必须**先同步更新对应设计文档**，再 commit（同一 commit 或同主题连续 commit）。
- **小迭代不发版**，无需打 tag。
- **发版由用户主动发起**：用户明确说"发版"时，按 §5 流程打 tag。
- 一切 commit 必须符合 §2 的 commit message 规范。

---

## 2. Commit Message 规范（Conventional Commits）

格式：

```
<type>(<scope>): <subject>

<body>

<footer>
```

### 2.1 type（必须）

| type | 用途 |
| --- | --- |
| `feat` | 新功能 / 新玩法 |
| `fix` | 修复 bug |
| `docs` | 仅文档变更（设计文档 / README / AGENTS / 注释） |
| `style` | 不影响逻辑的格式调整（空格、分号、缩进等） |
| `refactor` | 重构，不新增功能不修 bug |
| `perf` | 性能优化 |
| `test` | 测试相关 |
| `chore` | 构建、依赖、配置、脚本 |
| `revert` | 回滚 |

### 2.2 scope（可选）

指明改动范围，建议使用：
- `core`（核心循环）
- `ui`
- `data`（数据 / 配置 / 文案）
- `save`（存档 / 时间流）
- `friend`（道友）
- `alchemy`（炼丹）
- `reincarnation`（转生）
- `docs`（设计文档）
- `chore`（杂项）

### 2.3 subject

- 中文，简洁，**不超过 50 字**。
- 不加句号。
- 用动词开头："添加"、"修复"、"调整"、"移除"。

### 2.4 body（可选）

- 解释改动的**原因**和**做了什么**，不要重复 subject。
- 玩法 / 数值改动时，简述调整逻辑（让 reviewer 能理解）。
- 设计文档变更时，标注"对应文档：xx.md §X"。

### 2.5 footer（可选）

- `Refs: 设计文档/xx.md` —— 引用被更新的设计文档。
- `BREAKING CHANGE: <描述>` —— 破坏性变更必须标注。

### 2.6 示例

```
feat(alchemy): 添加筑基丹丹方与材料配方

玩家在金丹期可炼制筑基丹，提升突破成功率 20%。
对应文档：设计文档/04-炼丹系统.md §3.2
Refs: 设计文档/02-数值与境界.md
```

```
docs: 修正道友好感度阈值描述

之前写错阈值（应是 50 而非 40），本次修正。
Refs: 设计文档/05-道友系统.md
```

```
fix(save): 修复离线报告重复触发的问题
```

---

## 3. 玩法 / 策略改动时的强制流程

当用户提出**玩法、机制、数值、系统设计**相关的迭代要求时：

1. **先讨论**（如有必要，向用户提问澄清）。
2. **更新设计文档**（找到对应章节，改动 + 在 commit message 中引用）。
3. **实现代码**。
4. **同一主题的文档 + 代码可以在一个或一组连贯 commit 中提交**，主题用一致的 `feat` / `fix` / `refactor` type，scope 区分文档 / 代码部分。
5. **如有数据 / 数值变更**，确保 `设计文档/02-数值与境界.md` 或对应系统的数值表同步更新。

> 原则：**代码和文档不能脱节超过一个 commit cycle**。如果用户口头说了改动而文档还没更新，应该在下一次 commit 中补齐，并在 commit body 中说明。

---

## 4. 自动 Commit 行为

AI 每次完成一次"工作单元"（一段代码改动、一次文档修订）后：

1. 检查 `git status` 确认待提交文件。
2. 检查改动是否符合 commit 规范。
3. `git add` 对应文件。
4. `git commit -m "<规范的 commit message>"`。
5. **如有远程**，`git push`。
6. 报告给用户：
   - commit hash 简写
   - commit message
   - 涉及的文件清单
   - 是否需要用户 review

> 不要批量打包多个不相关改动进同一个 commit。一个 commit 一个主题。

---

## 5. 发版流程

**发版由用户触发**。用户说"发版"时：

1. AI 检查 `git status` 确保 working tree 干净。
2. AI 与用户确认版本号（基于当前阶段的 semver）。
3. `git tag -a vX.Y.Z -m "<版本说明>"`。
4. `git push origin vX.Y.Z`。
5. 在根目录 `CHANGELOG.md` 追加一段（详见 §6）。

### 5.1 版本号规则

遵循 semver：
- **0.x.y** —— MVP / 早期迭代，结构不稳定。
- **x.0.0** —— 重大里程碑（如 MVP 完成、1.0 上线、2.0 上线）。
- **x.y.0** —— 新功能 / 新系统加入。
- **x.y.z** —— 修复 / 调整。

### 5.2 版本号约定（项目当前）

| 阶段 | 范围 |
| --- | --- |
| MVP | 0.1.0 ~ 0.9.x |
| 1.0 | 1.0.0 |
| 1.x | 1.1.0 ~ 1.9.x |
| 2.0 | 2.0.0 |
| 2.x | 2.1.0 ~ 2.9.x |

### 5.3 发版时的额外动作

- 在 `CHANGELOG.md` 写入本次版本的变更摘要。
- 在 README 顶部更新"当前版本"标识。
- 设计文档如有重大变更，确保引用版本号。

---

## 6. CHANGELOG 维护

根目录 `CHANGELOG.md` 按以下格式记录：

```markdown
# 更新日志

## [v0.1.0] - 2026-XX-XX

### 新增
- 道友识别与好感度系统
- 30+ 材料与 25 种丹方

### 修复
- 离线报告重复触发

### 变更
- 调整练气期突破成功率从 80% 至 90%

---

## [v0.0.1] - 2026-XX-XX

### 新增
- 项目初始化
- 设计文档 v1
```

每次发版时按时间倒序追加。

---

## 7. AI 行为约束

- **不要自作主张发版**（不打 tag）。
- **不要把 commit 留在 working tree 不提交**。
- **不要在 commit message 中使用 emoji**（除非用户明确要求）。
- **不要 force push** 到 main。
- **不要更新全局 git config**；只使用项目级 `git config --local`。
- **遇到权限 / 凭证问题**时停下来询问用户，不要尝试绕过。
- 任何对游戏**平衡性**的猜测（"可能玩家会觉得太强 / 太弱"）应明确标记为"建议"，让用户决定。

---

## 8. 文档同步触发清单

AI 在每次会话开始 / 改动前，参考以下清单：

| 触发场景 | 需要更新的文档 |
| --- | --- |
| 修改境界 / 突破 / 寿元 | `02-数值与境界.md` |
| 修改修炼 / 冒险 / 炼丹机制 | `03-修炼与冒险.md` 和 / 或 `04-炼丹系统.md` |
| 修改道友系统 | `05-道友系统.md` |
| 修改转生 / 结局 | `06-转生与多结局.md` |
| 修改离线 / 时间 | `07-离线与时间.md` |
| 修改 UI / 视觉 / 大事记 | `08-界面与UX.md` |
| 修改分期范围 | `09-分期路线图.md` |
| 修改工作流程本身 | 本文件 + `WORKFLOW.md` + `README.md` 索引 |

---

## 9. 与用户的对话约定

- AI **不要替用户决定**设计方向。每个有争议的设计选择都应该问。
- AI **不要静默调整数值**。任何数值改动都必须在 commit body 中说明并让用户审阅。
- AI **不要写未在设计文档中提到的"隐藏机制"**。所有机制都先入文档，再入代码。
- AI **回复要简洁**，每次 commit 后只汇报必要信息。