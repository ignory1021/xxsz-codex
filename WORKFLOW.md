# WORKFLOW.md

本文件描述项目维护者（用户）视角的工作流。AI 视角的工作约定见 `AGENTS.md`。

---

## 1. 一次典型改动流程

```
[提出改动需求]
      │
      ▼
[AI 与用户讨论设计]
      │
      ▼
[更新设计文档]  ← 如涉及玩法 / 数值
      │
      ▼
[实现代码]
      │
      ▼
[AI 自动 commit + push]
      │
      ▼
[用户在 GitHub review]
      │
      ▼
[小迭代：不发版]
[大版本：用户说"发版" → AI 打 tag + 更新 CHANGELOG]
```

---

## 2. Commit message 规范

使用 **Conventional Commits**（社区标准），格式：

```
<type>(<scope>): <subject>
```

| type | 何时用 |
| --- | --- |
| `feat` | 新功能 |
| `fix` | 修复 |
| `docs` | 文档变更 |
| `style` | 格式调整 |
| `refactor` | 重构 |
| `perf` | 性能 |
| `chore` | 构建 / 依赖 / 配置 |

详细规范见 `AGENTS.md §2`。

---

## 3. 发版流程（用户主动触发）

当你想发版时，对 AI 说"发版"。然后：

1. AI 确认 working tree 干净。
2. AI 跟你确认版本号（semver）。
3. AI 打 tag：`v0.1.0`。
4. AI 推 tag 到 GitHub。
5. AI 在 `CHANGELOG.md` 追加一段。
6. AI 更新 `README.md` 顶部的"当前版本"。

### 版本号规则

- **0.x.y** —— MVP / 早期迭代。
- **x.0.0** —— 重大里程碑。
- **x.y.0** —— 新功能。
- **x.y.z** —— 修复 / 调整。

---

## 4. 玩法 / 数值改动的强制约束

任何影响 **玩法机制 / 数值 / 系统设计** 的改动，AI 必须：

1. **先讨论**（问清楚再动）。
2. **同步更新设计文档**（不是只改代码）。
3. **commit message** 引用被更新的设计文档章节。

不允许出现"代码改了，文档没跟上"超过一个 commit cycle。

---

## 5. Git 操作约束

- **永远 main 分支**，无 feature 分支。
- **不 force push** 到 main。
- **不发版不打 tag**。
- commit message **不加 emoji**。
- 全局 git config 不动，**仅项目级配置**。

---

## 6. 文档同步触发清单

| 你改了…… | AI 应同步更新…… |
| --- | --- |
| 境界 / 突破 / 寿元 | `02-数值与境界.md` |
| 修炼 / 冒险 / 炼丹 | `03` 和 / 或 `04` |
| 道友 | `05-道友系统.md` |
| 转生 / 结局 | `06-转生与多结局.md` |
| 离线 / 时间 | `07-离线与时间.md` |
| UI / 视觉 / 大事记 | `08-界面与UX.md` |
| 分期范围 | `09-分期路线图.md` |
| 工作流程本身 | `AGENTS.md` + 本文件 + README 索引 |

---

## 7. 文档索引

- `设计文档/README.md` —— 设计文档总览
- `设计文档/01-核心循环.md` —— 核心玩法闭环
- `设计文档/02-数值与境界.md` —— 寿元 / 灵根 / 境界 / 突破
- `设计文档/03-修炼与冒险.md` —— 行为机制
- `设计文档/04-炼丹系统.md` —— 材料 / 丹方 / 丹药
- `设计文档/05-道友系统.md` —— 识别 / 好感 / 组队 / 死亡
- `设计文档/06-转生与多结局.md` —— 转生 / 多结局 / 记录档
- `设计文档/07-离线与时间.md` —— 离线 / 暂停 / 推进
- `设计文档/08-界面与UX.md` —— 视觉 / 页面 / 大事记
- `设计文档/09-分期路线图.md` —— MVP / 1.x / 2.x 规划
- `AGENTS.md` —— AI 工作约定
- `CHANGELOG.md` —— 版本变更记录（发版时维护）