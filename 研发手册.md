# 修仙手札 · 研发手册

本手册汇总本项目已确认的研发、GitHub 和网页试玩规则，供维护者与 AI 协作时快速查阅。

如本手册与最新设计文档发生冲突，以用户最新确认的设计文档为准；如本手册与 [AGENTS.md](./AGENTS.md) 的 AI 执行约束冲突，以 `AGENTS.md` 为准。

---

## 1. 项目与仓库

| 项 | 约定 |
| --- | --- |
| GitHub 仓库 | [ignory1021/xxsz-codex](https://github.com/ignory1021/xxsz-codex) |
| 仓库可见性 | 公开 |
| 主分支 | `main` |
| 在线试玩 | [GitHub Pages](https://ignory1021.github.io/xxsz-codex/) |
| 本地试玩工程 | `app/` |
| 首发平台 | 手机浏览器（响应式网页） |
| 后续平台 | Android APK（Capacitor 打包） |

仓库不提交密钥、令牌、个人数据、构建产物或本地存档。公开 Pages 中也不得展示调试信息或私密内容。

---

## 2. 日常研发流程

每次工作按以下顺序进行：

1. 阅读 `AGENTS.md`、本手册和相关最新设计文档；检查 `git status`，不得覆盖用户未提交的改动。
2. 涉及玩法、数值、系统或叙事规则时，先与用户确认有争议的选择，再更新对应设计文档。
3. 完成实现后，执行与改动相关的测试、类型检查和构建。
4. 每个独立工作单元完成后，立即 commit 并 push 到 `origin/main`。
5. 汇报 commit 短 hash、提交信息、涉及文件、验证结果和是否仍需要用户审核。

代码与设计文档不得脱节超过一个 commit cycle。数值与平衡性方案必须明确标注“建议”或“待审核”；未经用户确认不得作为隐藏机制进入代码。

### 2.1 设计文档同步

| 改动内容 | 必须同步的文档 |
| --- | --- |
| 核心循环、行动时间、资源结算 | `设计文档/01-核心循环.md`、`09-炼丹主线与经济.md` |
| 寿元、灵根、境界、突破 | `设计文档/02-数值与境界.md`、`10-MVP数值草案.md` |
| 探索、文字战斗、调息、奇遇 | `设计文档/03-修炼与冒险.md` |
| 材料、丹方、丹药、炼丹 | `设计文档/04-炼丹系统.md`、`10-MVP数值草案.md` |
| 道友、同行、好感、重伤 | `设计文档/05-道友系统.md`、`10-MVP数值草案.md` |
| 转生、飞升、结局、世系 | `设计文档/06-转生与多结局.md` |
| UI、交互、大事记 | `设计文档/07-界面与UX.md` |
| MVP 范围与分期 | `设计文档/08-分期路线图.md` |
| 工作约定 | 本手册、`AGENTS.md`、`WORKFLOW.md`、根 `README.md` |

---

## 3. GitHub 与提交推送

### 3.1 分支和安全规则

- 日常改动直接提交到 `main`，不创建长期 feature 分支。
- 不执行 force push、`git reset --hard`、未经用户确认的回滚或删除。
- 不修改全局 Git 配置；确有必要时仅使用项目级 `git config --local`。
- 凭证、权限或远程推送失败时停止并告知用户，不尝试绕过。
- 一个 commit 只处理一个主题，不混入无关格式改动或用户尚未确认的内容。

### 3.2 Commit 规范

使用 Conventional Commits：

```text
<type>(<scope>): <中文动词开头的简短说明>
```

可用 `type`：

| type | 用途 |
| --- | --- |
| `feat` | 新功能、新玩法 |
| `fix` | Bug 修复 |
| `docs` | 文档、说明、注释 |
| `style` | 不影响逻辑的格式调整 |
| `refactor` | 重构 |
| `perf` | 性能优化 |
| `test` | 测试 |
| `chore` | 依赖、构建、配置、脚本 |
| `revert` | 回滚 |

常用 `scope`：`core`、`ui`、`data`、`save`、`friend`、`alchemy`、`reincarnation`、`docs`、`chore`。

- subject 使用中文、动词开头、不超过 50 字、不加句号、不使用 emoji。
- 玩法与数值提交的 body 要写明调整原因和数值逻辑。
- 设计文档提交的 body 要写“对应文档：文件名 §章节”。
- 必要时以 `Refs: 设计文档/xx.md` 标注关联文档；破坏性变更使用 `BREAKING CHANGE:`。

### 3.3 推送规则

每次代码、配置、资源或文档改动完成后：

```bash
git status
git add <本次相关文件>
git commit -m "<符合规范的提交信息>"
git push origin main
```

推送前应确认待提交文件只属于当前主题。推送后应检查远程返回结果；不得将未提交改动留在工作区。

---

## 4. 发版与 GitHub Pages

### 4.1 发版

- 只有用户明确提出“发版”时才能发版和打 tag。
- 小迭代只 commit + push，不打 tag。
- 发版流程：确认工作区干净 → 与用户确认 semver 版本号 → 创建附注 tag `vX.Y.Z` → 推送 tag → 更新 `CHANGELOG.md` → 更新根 README 当前版本。

版本规则：`0.x.y` 用于 MVP 与早期迭代；`x.y.0` 表示新功能；`x.y.z` 表示修复或小调整；`x.0.0` 表示重大里程碑。

### 4.2 GitHub Pages

- `.github/workflows/deploy-pages.yml` 在推送 `main` 或手动触发时自动执行。
- 工作流使用 Node.js 22，在 `app/` 中依次执行 `npm ci`、`npm test`、`npm run build`。
- 构建时必须设置 `VITE_BASE_PATH=/xxsz-codex/`，保证静态资源在 Pages 子路径下正常加载。
- 测试或构建失败时不得视为发布成功；先修复并再次推送。
- Pages 发布由 GitHub Actions 完成，不手动维护 `gh-pages` 分支或上传构建文件。
- PWA 使用更新提示策略；版本更新后应提示试玩用户刷新，而不是静默替换正在进行的游戏。

---

## 5. 网页试玩版工程规则

### 5.1 技术栈与目录

| 层 | 约定 |
| --- | --- |
| UI | React 19 + TypeScript |
| 构建 | Vite |
| 状态 | Zustand |
| PWA | `vite-plugin-pwa` |
| 存档 | 浏览器 localStorage；Android 使用 Capacitor Preferences 适配 |
| Android | Capacitor，`appId` 为 `com.ignory1021.xxsz` |
| 测试 | Vitest + Testing Library |

目录职责：

- `app/src/core/`：与 UI 无关的规则、时间、随机数和领域类型；优先编写单元测试。
- `app/src/data/`：地图、材料、丹方、装备、事件等手工数据配置。
- `app/src/store/`：游戏状态、行动编排和 UI 协调。
- `app/src/persistence/`：网页与 Android 共用的存档接口。
- `app/src/components/`：移动端页面与交互。
- `app/src/styles/`：古风水墨主题与响应式样式。

### 5.2 本地命令

在 `app/` 目录执行：

```bash
npm install
npm run dev
npm run typecheck
npm test
npm run build
npm run preview
```

Android 打包前执行：

```bash
npm run cap:add:android
npm run cap:sync
npm run cap:open:android
```

Android 原生工程由 Android Studio 负责构建 APK；每次网页资源变更后先运行 `npm run cap:sync`。

### 5.3 质量门槛

- 修改 `core`、`store` 或存档逻辑时，补充或更新对应单元测试。
- 提交前至少运行受影响测试；涉及网页构建、依赖或配置时运行 `npm run typecheck`、`npm test`、`npm run build`。
- 在手机宽度 `360 ~ 414px` 验证主要页面；底部导航必须与主内容容器左右对齐。
- 验证刷新后的 localStorage 存档可恢复；不得因普通 UI 更新丢失现有存档。
- 所有随机判定使用统一随机工具，确保测试可注入或固定随机源。

---

## 6. 试玩版玩法与交互边界

### 6.1 当前核心规则

- 游戏是**主动行动制**，不是挂机或放置游戏。
- 年龄仅在探索、炼丹、调息等行动结算时推进；不随现实时间、离线、开始/暂停或倍率自动变化。
- 1 年 = 360 天，1 月 = 30 天；基础、中阶、高阶行动分别在 `20~40`、`40~80`、`60~120` 天内随机结算。
- 行动先结算物品、丹道经验和可能触发的层级提升，再推进寿元；寿元不足仍可行动，结算后才进入当世结局。
- 突破由玩家手动发起，不消耗寿元；渡劫失败返回渡劫大圆满。
- 五行灵根只包含金、木、水、火、土；每次转世完整重随机，不继承灵根或其调整效果。
- 悟性与悟道点不进入当前 MVP。

数值、公式、地图、材料、丹方、装备与道友同行的待审核基线见 [设计文档/10-MVP数值草案.md](./设计文档/10-MVP数值草案.md)。未经用户审核确认的草案不得写入游戏逻辑。

### 6.2 用户体验规则

- 玩家只需主动选择一次探索、炼丹、调息或集市操作；普通结算自动写入修行历程，不再弹出确认框。
- 奇遇与道友相逢使用剧情卡并要求玩家选择；选项事前不展示数值、成功率或关系结果。
- 死亡、转生、飞升和其他不可逆重大事件保留用户确认。
- 界面保持手机优先、浅色古风水墨风格；主要按钮触达区充足，底部导航固定且与内容容器对齐。
- 数值结果、随机实际消耗、失败原因和资源不足原因必须可见；不做隐藏扣费、自动购买或自动出售。

### 6.3 内容制作规则

- MVP 覆盖练气至飞升的全部境界，采用 9 张地图、30 种材料、25 张丹方、30~50 名道友、8 类结局与 100~200 段叙事文案。
- 地图、材料、丹方、装备、道友和文案均手工编写并交由用户审核精修，不使用自动生成的模板内容替代正式内容。
- 游戏数值与随机机制必须先写入设计文档；调整时同步更新数值草案和关联系统文档。

---

## 7. 相关文档

- [设计文档总览](./设计文档/README.md)
- [MVP 数值草案](./设计文档/10-MVP数值草案.md)
- [AI 工作约定](./AGENTS.md)
- [用户工作流程](./WORKFLOW.md)
- [更新日志](./CHANGELOG.md)
